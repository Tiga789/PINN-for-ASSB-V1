# GV1 D12 TRUE SMOKE rescue scorecard fix

Purpose: recover metrics from existing D12 TRUE_SMOKE run directories after the
collector reported `read_error` because `d10_voltage_metrics.json` was missing.

This is a read-only rescue. It does not train or modify D9.6/D9.5.1 mainline.
